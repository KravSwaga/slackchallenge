Readme
Slack Config Project

- Requires Python3.9
- Run bootstrap.sh to install required non-default Python packages
- Should be run on Linux systems only as Python Pexpect module doesn't work on Windows
- Modify config.json to change parameters